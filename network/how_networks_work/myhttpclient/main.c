#include <stdio.h>

int main() {
  // ドメイン名からIPアドレスを取得する
  // ソケットを作成し、接続先を登録する
  // ソケットに接続する
  // http requestを実行する
  // responseを読み込む
  // closeする
  int number = 1;
  printf("hi %d", number);
  return 0;
}
